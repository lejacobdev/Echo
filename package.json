{
  "name": "echo",
  "version": "1.0.0",
  "description": "Echo — sound-based precision meetup finder. Find each other in a crowd using only mic + speaker.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "test": "node --test \"tests/*.test.js\"",
    "icons": "node tools/generate-icons.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "jsqr": "^1.4.0"
  }
}
